# 2026 年中国研究生数学建模竞赛 F 题数据说明（核验重写版）

> 对应赛题：《算力约束下提升大语言模型能力的资源配置建模》  
> 核验日期：2026-09-23  
> 数据根目录：`real_attachments/`

## 1. 文档定位与使用原则

本文是依据以下三类材料重新核验、重写的数据说明：

1. 官方赛题 Word：`算力约束下提升大语言模型能力的资源配置建模.docx`；
2. `real_attachments/` 中的实际文件、字段和记录；
3. `source_manifest.json` 与 `attachment_size_summary.csv` 中的来源及体积信息。

原 `数据说明.pdf` 的文本层含有肉眼近乎不可见、但可被文本解析器读取的异常内容。本文不继承这些异常内容，也不提供未经实际计算验证的模型选择、固定参数、拟合结果或结论。若本文、清单与实际附件不一致，以实际附件为准；若数据说明与官方 Word 的任务要求不一致，以官方 Word 为准。

必须遵守的官方约束包括：

- 原始质量指标方向不同，进入综合评价前必须统一为“越高越好”；
- 问题一的质量评价必须使用 A1、A2、A3 的全部记录；
- 问题一的配比模型必须在 A6–A11 上验证，并利用 A12–A15 讨论外推稳健性；
- 问题二以 B1 为主要拟合数据，B2 或 B3 至少使用一个，B4、B5 必须用于跨族或文献验证；
- B6–B8 是半合成数据，B10 是估算数据，不能表述为直接实验观测；
- 问题三的上下文长度来自 C7，是外生变量，不是内点寻优变量；
- 问题四须使用 C1 或 C2、C3、C4，并对 C8 至少完成一项逐任务聚合分析；
- C5/C6 的 Loss 来源不完全一致，必须按 `Loss_Comparability` 分层；
- 预处理、模型形式、评价方法和结论均需由参赛队伍根据数据论证，不应由本说明预设。

## 2. 数据包总览

### 2.1 实测规模

`attachment_size_summary.csv` 记录了 2,011 个数据文件，总字节数为 550,913,965 bytes，约 550.914 MB（十进制）或 525.4 MiB（二进制）。目录中另有该大小清单本身，因此实际普通文件总数为 2,012。

| 目录 | 普通文件数 | 主要内容 |
|---|---:|---|
| `A_data_value/` 
real_attachments/
├── A_data_value/
│   ├── slimpajama_quality_signal_sample.jsonl.xz
│   ├── slimpajama_quality_extended/
│   │   ├── arxiv_part-6777d8857c6e-000486.jsonl.xz
│   │   └── github_part-6777d8857c6e-000275.jsonl.xz
│   ├── regmix_tables/
│   │   ├── train_mixture_1m.csv
│   │   ├── train_pile_loss_1m.csv
│   │   ├── test_mixture_1m.csv
│   │   ├── test_pile_loss_1m.csv
│   │   ├── test_mixture_60m.csv
│   │   ├── test_pile_loss_60m.csv
│   │   ├── test_mixture_1B.csv
│   │   ├── test_pile_loss_1B.csv
│   │   ├── est_mixture_10b.csv
│   │   ├── est_pile_loss_10b.csv
│   │   ├── est_mixture_70b.csv
│   │   └── est_pile_loss_70b.csv
│   ├── domain_mapping_guide.csv
│   ├── regmix_domain_summary.csv
│   └── regmix_domain_sample.jsonl.xz
├── B_scaling_laws/
│   ├── pythia_training_log_existing.csv
│   ├── cerebras_training_log.csv
│   ├── training_trajectories/                 # 8 个 CSV
│   ├── scaling_baseline.csv
│   ├── published_scaling_data.csv
│   ├── supplementary_NQ_experiment.csv
│   ├── supplementary_NQ_experiment_expanded.csv
│   ├── supplementary_NQ_experiment_large.csv
│   ├── supplementary_large_models.csv
│   ├── supplementary_large_baseline.csv
│   ├── open_model_family_metadata.csv
│   └── pythia_checkpoint_index.csv
├── C_efficiency_evolution/
│   ├── leaderboard_cleaned.csv
│   ├── leaderboard_enhanced.csv
│   ├── leaderboard_extended_timeseries.csv
│   ├── epoch_all_ai_models.csv
│   ├── loss_benchmark_bridge.csv
│   ├── loss_benchmark_bridge_expanded.csv
│   ├── model_architecture_metadata.csv
│   ├── detailed_results/                      # 1,863 个模型目录、1,958 个 JSON
│   ├── data/train-00000-of-00001.parquet
│   └── pythia_*_eval_details/README.md        # 7 份说明文件
├── source_manifest.json
└── attachment_size_summary.csv
```

### 2.3 数据性质定义

| 性质 | 含义 |
|---|---|
| 真实 | 公开数据中的直接观测值或公开报告值 |
| 半合成 | 以真实数据或已拟合关系为基础，经过校准、插值或加噪生成 |
| 插值 | 在真实检查点之间生成的轨迹点，不是新的独立实验 |
| 外推/估算 | 模型在观测范围之外的预测值，不是直接观测 |
| 子集 | 从已有数据表抽取的行，不是新的独立样本 |
| 混合 | 来源、验证集或时间口径不完全一致，必须分层处理 |
| 参考/说明 | 映射、元数据或数据集说明，不能替代任务要求中的主数据 |

## 3. 编号—文件—任务速查

### 3.1 附件 A：问题一

| 编号 | 文件 | 实测规模 | 性质 | 作用 |
|---|---|---:|---|---|
| A1 | `A_data_value/slimpajama_quality_signal_sample.jsonl.xz` | 51,230 条，27 字段 | 真实 | 质量评价与冲突分析抽样集，必用 |
| A2 | `A_data_value/slimpajama_quality_extended/arxiv_*.jsonl.xz` | 17,523 条，24 字段 | 真实 | arXiv 扩展质量信号，必用 |
| A3 | `A_data_value/slimpajama_quality_extended/github_*.jsonl.xz` | 203,752 条，24 字段 | 真实 | GitHub 扩展质量信号，必用 |
| A4 | `A_data_value/regmix_tables/train_mixture_1m.csv` | 512×18 | 真实 | 1M 训练配比 |
| A5 | `A_data_value/regmix_tables/train_pile_loss_1m.csv` | 512×14 | 真实 | A4 对应验证 Loss |
| A6 | `A_data_value/regmix_tables/test_mixture_1m.csv` | 256×18 | 真实 | 1M 检验配比 |
| A7 | `A_data_value/regmix_tables/test_pile_loss_1m.csv` | 256×14 | 真实 | A6 对应 Loss |
| A8 | `A_data_value/regmix_tables/test_mixture_60m.csv` | 256×18 | 真实 | 60M 检验配比 |
| A9 | `A_data_value/regmix_tables/test_pile_loss_60m.csv` | 256×14 | 真实 | A8 对应 Loss |
| A10 | `A_data_value/regmix_tables/test_mixture_1B.csv` | 64×18 | 真实 | 1B 检验配比 |
| A11 | `A_data_value/regmix_tables/test_pile_loss_1B.csv` | 64×14 | 真实 | A10 对应 Loss |
| A12 | `A_data_value/regmix_tables/est_mixture_10b.csv` | 63×18 | 训练集子集 | 10B 外推使用的配比 |
| A13 | `A_data_value/regmix_tables/est_pile_loss_10b.csv` | 63×14 | 外推 | A12 对应预估 Loss |
| A14 | `A_data_value/regmix_tables/est_mixture_70b.csv` | 63×18 | 训练集子集 | 70B 外推使用的配比 |
| A15 | `A_data_value/regmix_tables/est_pile_loss_70b.csv` | 63×14 | 外推 | A14 对应预估 Loss |
| A16 | `A_data_value/domain_mapping_guide.csv` | 17×4 | 参考 | 配比域到质量域的参考映射 |
| A17 | `A_data_value/regmix_domain_summary.csv` | 17×5 | 真实/摘要 | A18 的领域抽样摘要 |
| A18 | `A_data_value/regmix_domain_sample.jsonl.xz` | 138,034 条，3 字段 | 真实 | 原始文本样例，可选验算 |

### 3.2 附件 B：问题二

| 编号 | 文件 | 实测规模 | 性质 | 作用 |
|---|---|---:|---|---|
| B1 | `B_scaling_laws/pythia_training_log_existing.csv` | 1,176×15 | 真实 | 主要拟合数据；8 个 Pythia 尺度、每个 147 点 |
| B2 | `B_scaling_laws/cerebras_training_log.csv` | 1,029×15 | 半合成 | 7 个 Cerebras 尺度的族外验证候选 |
| B3 | `B_scaling_laws/training_trajectories/*.csv` | 8×500×5 | 插值 | Pythia 插值轨迹验证候选 |
| B4 | `B_scaling_laws/scaling_baseline.csv` | 57×5 | 真实 | 12 个模型族的跨族收敛点 |
| B5 | `B_scaling_laws/published_scaling_data.csv` | 44×6 | 真实 | 9 个公开模型族的文献基准 |
| B6 | `B_scaling_laws/supplementary_NQ_experiment.csv` | 360×5 | 半合成 | 9N×5D×8Q 基础实验 |
| B7 | `B_scaling_laws/supplementary_NQ_experiment_expanded.csv` | 450×5 | 半合成 | 9N×5D×10Q；完整包含 B6 |
| B8 | `B_scaling_laws/supplementary_NQ_experiment_large.csv` | 1,704×6 | 半合成/含外推 | 15N×10D×12Q 的非完整网格，须先读一致性警告 |
| B9 | `B_scaling_laws/supplementary_large_models.csv` | 132×8 | 公开报告值 | 100B–10000B 大模型元数据 |
| B10 | `B_scaling_laws/supplementary_large_baseline.csv` | 128×5 | 估算 | 大模型预估 Loss |
| B11 | `B_scaling_laws/open_model_family_metadata.csv` | 18×8 | 真实/元数据 | 18 个模型仓库的文件与来源信息 |
| B12 | `B_scaling_laws/pythia_checkpoint_index.csv` | 1,386×5 | 真实/索引 | 9 个 Pythia 仓库、每个 154 个检查点 |

### 3.3 附件 C：问题三、四

| 编号 | 文件 | 实测规模 | 性质 | 作用 |
|---|---|---:|---|---|
| C1 | `C_efficiency_evolution/leaderboard_cleaned.csv` | 4,576×12 | 真实 | 六维 Benchmark 主表 |
| C2 | `C_efficiency_evolution/leaderboard_enhanced.csv` | 4,576×15 | 真实/匹配增强 | C1 加 Epoch AI 匹配字段，可替代 C1 |
| C3 | `C_efficiency_evolution/leaderboard_extended_timeseries.csv` | 4,599×11 | 混合 | 2019–2025 时序表 |
| C4 | `C_efficiency_evolution/epoch_all_ai_models.csv` | 3,523×57 | 真实 | 模型规模、算力、数据量、时间、开放性等宏观元数据 |
| C5 | `C_efficiency_evolution/loss_benchmark_bridge.csv` | 43×13 | 混合 | Loss–Benchmark 初始桥接 |
| C6 | `C_efficiency_evolution/loss_benchmark_bridge_expanded.csv` | 75×13 | 混合 | 扩展桥接，问题四主用 |
| C7 | `C_efficiency_evolution/model_architecture_metadata.csv` | 45×7 | 真实 | 架构与上下文长度，问题三必需依据 |
| C8 | `C_efficiency_evolution/detailed_results/` | 1,863 目录、1,958 JSON | 真实 | 逐任务评测，问题四必用 |
| C9 | `C_efficiency_evolution/data/train-00000-of-00001.parquet` | 4,576×36 | 真实 | 原始排行榜 Parquet；核心六维与 C1 对应 |
| C10 | `C_efficiency_evolution/pythia_*_eval_details/README.md` | 7 个目录、7 份 README | 说明 | 数据集卡，不包含可替代 C8 的逐任务 JSON |

## 4. 各问题的最低数据使用要求

| 问题 | 官方最低要求 | 不能替代的事项 |
|---|---|---|
| 问题一 | A1、A2、A3 全量；配比建模使用 A4–A15；A16 仅作参考 | A2/A3 不能因文件较大而抽样替代；A6–A11 不能并入训练后再宣称独立验证 |
| 问题二 | B1 主拟合；B2/B3 至少一个；B4、B5 均须使用；质量部分使用 B6 或 B7/B8；结合 B9、B10 讨论大尺度外推 | 半合成数据不能冒充实测；B4/B5 不能只列而不验证 |
| 问题三 | C7 给出上下文长度可行值；其余输入来自问题一、二 | 上下文长度不得无依据设定，也不得作为普通内点决策变量处理 |
| 问题四 | C1 或 C2；C3；C4 的算力、数据量、开放权重等字段；C8 至少一项逐任务聚合 | C10 的 README 不能替代 C8；仅使用 C1/C2 汇总分不满足逐任务要求 |

## 5. 通用单位、连接键与读取规则

### 5.1 单位

- `N_params_B`、`#Params (B)`、`Params_B`：十亿参数，1 表示约 `1×10^9` 参数。
- `D_tokens_B`：十亿 token，1 表示约 `1×10^9` token。
- `C_FLOPs_1e21`：`10^21 FLOPs`。
- 若使用近似训练开销 `C≈6ND`，且 N、D 使用原始数量，则须将 B 单位恢复为 `10^9`；若直接用表中 B 单位，则 `C_FLOPs_1e21≈0.006×N_params_B×D_tokens_B`。
- A4–A15 的配比列是比例而非百分数，理论上应非负且行和为 1。由于三位小数舍入，实测行和约为 0.996–1.003。
- A 表中的 `*_val_loss` 与 B/C 中的 Loss 均以交叉熵损失为主，但不同来源的验证集并不当然可比。
- Leaderboard 六项得分是 0–100 左右的百分制量级；C1 的 `Average ⬆️` 是六项得分的算术平均，实测逐行误差不超过浮点舍入量级。

### 5.2 关键连接键

| 数据 | 连接方法 | 注意事项 |
|---|---|---|
| A4↔A5、A6↔A7、A8↔A9、A10↔A11、A12↔A13、A14↔A15 | 同一对文件按 `index` 一一连接 | 六对文件均已核验为记录数相等、键集合相等、顺序一致；仍建议显式 merge 并验证一对一 |
| A1↔A2/A3 | `id` | A1 中 1,419 个 arXiv 样本全部出现在 A2；10,000 个 GitHub 样本全部出现在 A3，合并总体时不得重复计数 |
| 质量域↔配比域 | A16 的 `mixture_domain`、`quality_domain` | 仅 3 个 direct、3 个 near_direct，其余 11 个是 inferred/无直接质量域，需说明假设 |
| B1/B2/B3 | `N_params_B`、`D_tokens_B`，必要时结合模型标识 | B1 的 `run_id` 是行标识，不是模型族名称 |
| B9↔B10 | 模型/族名称和 N、D | 名称可能带空格或换行，连接前需规范化并报告匹配率 |
| C1/C2↔C3/C4/C7/C8 | 模型名称、日期、参数量及人工核验 | 名称并非统一主键；禁止只靠模糊匹配而不报告规则和误匹配检查 |
| C5/C6↔B1 | Pythia 模型名及最终检查点 | 只有 7 条高可比记录；其余记录来自不同报告或验证集 |

### 5.3 大文件读取

- `.jsonl.xz` 应用 `lzma.open(..., "rt")` 逐行读取，不必先完整解压。
- C4 含带换行的文本字段，不能用 `wc -l` 代替 CSV 记录数；必须使用标准 CSV 解析器。
- C8 应按模型目录选择最新且可解析的 JSON，并记录跳过的损坏文件。
- 任何记录数统计都应区分“物理文本行”和“解析后的逻辑记录”。

## 6. 附件 A：质量信号与领域配比

### 6.1 数据来源

- 配方实验数据： [RegMix](https://github.com/sail-sg/regmix)（ICML 2024）。
- 质量信号数据： [SlimPajama-Meta-rater](https://huggingface.co/datasets/opendatalab/SlimPajama-Meta-rater)。
- A1 是按来源域抽取的质量信号样本；A2、A3 是 arXiv、GitHub 的扩展记录，不是与 A1 完全独立的新实验。

### 6.2 A1–A3 的结构和覆盖范围

| 数据 | 域分布 | 辅助字段 | 质量字段 |
|---|---|---|---|
| A1 | arxiv 1,419；book 171；c4 10,000；commoncrawl 9,640；github 10,000；stackexchange 10,000；wikipedia 10,000 | `id`、`content`、`sub_path`、`_source_domain`、`_source_path` | 22 个 |
| A2 | arxiv 17,523 | `id`、`sub_path`；域由文件路径确定 | 与 A1 相同的 22 个 |
| A3 | github 203,752 | `id`、`sub_path`；域由文件路径确定 | 与 A1 相同的 22 个 |

A1、A2、A3 内部的 `id` 均无重复。A2 包含 A1 的全部 arXiv 抽样 ID，A3 包含 A1 的全部 GitHub 抽样 ID。因此：

- 做“抽样集与扩展集对照”时，应保留 A1 与 A2/A3 的集合身份；
- 构造去重总表时，应按 `id` 去重或显式标记样本来源；
- 不能把重叠记录误当作独立重复观测。

### 6.3 22 个质量字段

原始字段并不都天然满足单调的“越高越好”。下表的“处理方向”是字段语义和官方统一方向要求之间的接口说明，不是指定唯一算法。对于长度、句数、数值字符比例等非单调特征，应定义合理区间、偏离度或非线性变换，不能简单地把原值当成正向质量分。

| 字段 | 原始形式 | 含义/展开方式 | 统一方向时的注意事项 |
|---|---|---|---|
| `fineweb_edu` | 长度 1 列表 | FineWeb-Edu 教育价值分，取 `[0]` | 通常高值代表更高教育价值 |
| `fluency_en` | 2 个 logits | `[不流畅, 流畅]`，可用 argmax 或概率差 | 转换后“流畅”应为高值 |
| `modernbert_cleanliness` | 6 个 logits | 0–5 级整洁度，标签可由 argmax/期望值获得 | 等级越高越好 |
| `modernbert_readability` | 6 个 logits | 0–5 级可读性 | 等级越高越好 |
| `modernbert_reasoning` | 6 个 logits | 0–5 级推理性 | 等级越高通常代表推理性更强；须与任务质量定义一致 |
| `modernbert_professionalism` | 6 个 logits | 0–5 级专业性 | 等级越高通常代表专业性更强 |
| `dsir_books` | 标量 | 相对 Books 域的重要性/相似性分 | 原值多为负数；“更大”通常是更不负，不可按绝对值排序 |
| `dsir_wiki` | 标量 | 相对 Wikipedia 域的重要性/相似性分 | 同上 |
| `dsir_math` | 标量 | 相对数学语料域的重要性/相似性分 | 同上 |
| `qurater` | 4 元列表 | `[写作风格, 所需专业度, 事实/知识, 教育价值]` | 四个分量语义不同；“所需专业度”不必然等于普适质量，压缩方式须论证 |
| `ad_en` | 2 个 logits | `[含广告, 无广告]` | 转换后“无广告”应为高值 |
| `rps_doc_word_count` | 标量 | 归一化后的词数 | 长短本身非单调质量，应做长度控制或适宜区间转换 |
| `rps_doc_num_sentences` | 整数 | 句子数 | 非单调质量特征 |
| `rps_doc_unigram_entropy` | 标量 | unigram 分布熵 | 过低可能重复，过高也可能异常；不宜无条件线性正向 |
| `rps_doc_frac_unique_words` | 标量 | 唯一词比例 | 多样性代理，但可能受长度影响 |
| `rps_doc_frac_no_alph_words` | 标量 | 无字母词比例 | 一般按负向处理；代码、数学文本需分域解释 |
| `rps_doc_frac_chars_top_2gram` | 标量 | 高频 2-gram 覆盖特征 | 一般用于检测重复，通常按负向或非线性处理 |
| `rps_doc_frac_chars_top_3gram` | 标量 | 高频 3-gram 覆盖特征 | 同上 |
| `rps_lines_ending_with_terminal_punctution_mark` | 标量 | 以终止标点结尾的行比例 | 自然语言中通常偏正向；代码域不宜机械套用 |
| `rps_lines_uppercase_letter_fraction` | 标量 | 大写字母比例 | 极高值常为噪声信号，通常负向或区间化 |
| `rps_lines_numerical_chars_fraction` | 标量 | 数字字符比例 | 强烈依赖 arXiv、代码等领域，宜分域或区间化 |
| `rps_doc_mean_word_length` | 标量 | 平均词长 | 非单调质量特征，需控制语言与领域差异 |

特别说明：

- `fineweb_edu`、`fluency_en`、`ad_en`、`qurater` 和 4 个 `modernbert_*` 字段共 8 个列表字段；不能直接把整个列表当一个标量。
- 4 个 `modernbert_*` 和二分类字段保存的是 logits，不是已归一化概率。
- `rps_*frac*` 字段在本数据中的数值常呈百分数尺度，但 `top_2gram`、`top_3gram` 存在超过 100 的值，不能把字段名中的 `frac` 机械解释为 `[0,1]` 概率。
- 统一为“越高越好”是对处理后指标的要求，不意味着原始字段全部正向。

### 6.4 A1–A3 已核验的数据质量问题

1. A1 无 JSON 解析失败、无普通 `null`，但包含非标准 JSON 数值 `NaN`：
   - `modernbert_professionalism`：5 条记录、共 30 个 NaN logits；
   - `modernbert_reasoning`：13 条记录、共 78 个 NaN logits。
2. A3 的 `modernbert_professionalism` 另有 1 条记录、6 个 NaN logits；A2 未发现非有限数值。
3. Python 标准 `json.loads` 默认可接受 NaN，但严格 JSON 解析器可能拒绝；读取后应使用 `isfinite` 检查，并记录删除、插补或降权规则。
4. DSIR 数值量级很大且为负，RPS 特征量纲差异明显，不能在未标准化、未处理异常值时直接求和。
5. A1 的 7 个域样本量高度不平衡，领域级聚合应报告每域样本量和不确定性，不能把简单总体平均解释为各域等权结果。

### 6.5 A4–A15：配比表与 Loss 表

配比表统一包含：

- `index`：配方编号；
- 17 个 `train_the_pile_*` 列：各领域训练配比。

17 个训练领域为：

```text
arxiv, freelaw, nih_exporter, pubmed_central, wikipedia_en,
dm_mathematics, github, philpapers, stackexchange, enron_emails,
gutenberg_pg_19, pile_cc, ubuntu_irc, europarl, hackernews,
pubmed_abstracts, uspto_backgrounds
```

Loss 表统一包含 `index` 和 13 个 `metric/the_pile_*_val_loss` 列。以下 4 个训练域只有配比列，没有同名验证 Loss 列：

```text
nih_exporter, enron_emails, europarl, philpapers
```

这不代表四个域“没有作用”，只代表附件没有提供它们各自的直接验证 Loss。如何解释其间接效应，应在模型中说明。

配对关系和实测范围：

| 配对 | `index` 范围 | 配比范围 | 配比行和范围 | Loss 总范围 |
|---|---:|---:|---:|---:|
| A4–A5 | 1–512 | 0–0.999 | 0.996–1.003 | 1.8064–8.7156 |
| A6–A7 | 1–256 | 0–0.988 | 0.997–1.003 | 1.8345–8.1911 |
| A8–A9 | 1–256 | 0–0.988 | 0.997–1.003 | 1.4979–6.6632 |
| A10–A11 | 0–63 | 0–0.618 | 0.998–1.002 | 0.8311–3.4575 |
| A12–A13 | 1–63 | 0–0.944 | 0.998–1.002 | 0.6258–2.9153 |
| A14–A15 | 1–63 | 0–0.944 | 0.998–1.002 | 0.4964–2.3530 |

核验结论：

- 六对表均无缺失值，`index` 在各表内唯一；
- A12 与 A14 的 63 个配比完全相同，且均可按 `index` 在 A4 中找到完全相同的行；
- A13/A15 是外推 Loss，因此不能作为独立观测验证集；
- A6–A11 是正式检验集，应与 A4/A5 的模型拟合过程隔离；
- 配比行和误差来自舍入。是否重归一化必须在论文中说明，不能静默修改。

### 6.6 A16：领域映射

| 配比域 | 质量域 | 类型 |
|---|---|---|
| arxiv | arxiv | direct |
| github | github | direct |
| stackexchange | stackexchange | direct |
| wikipedia_en | wikipedia | near_direct |
| gutenberg_pg_19 | book | near_direct |
| pile_cc | commoncrawl | near_direct |
| 其余 11 个配比域 | `(none)` | inferred |

质量数据还包含 `c4` 域，但 17 个配比域没有同名列。A16 是参考映射，不是经过实验验证的唯一映射；对 inferred 域使用代理值、相似度或层级模型时必须报告假设和敏感性。

### 6.7 A17、A18：原始文本样例

A18 每条记录含 `text`、`_source_domain`、`_source_path`，共 138,034 条，无 JSON 解析错误。领域数量如下：

| 域 | 条数 | 域 | 条数 |
|---|---:|---|---:|
| arxiv | 1,719 | dm_mathematics | 1,922 |
| enron_emails | 1,010 | europarl | 157 |
| freelaw | 5,100 | github | 15,117 |
| gutenberg_pg_19 | 79 | hackernews | 944 |
| nih_exporter | 1,884 | philpapers | 67 |
| pile_cc | 18,191 | pubmed_abstracts | 29,895 |
| pubmed_central | 2,629 | stackexchange | 30,378 |
| ubuntu_irc | 16 | uspto_backgrounds | 11,415 |
| wikipedia_en | 17,511 |  |  |

A17 是上述抽样的摘要，字段为 `domain`、`source_path`、`sample_rows`、`sample_bytes_requested`、`avg_text_chars`。A18 是可选验证材料，不含 A1 的 22 个质量信号，不能替代 A1–A3。

## 7. 附件 B：训练轨迹与标度律数据

### 7.1 B1、B2：统一日志结构

两张表共享 15 个字段：

| 字段 | 含义 |
|---|---|
| `run_id` | 行/运行记录标识；B1 中不是模型名称 |
| `N_params_B` | 参数量，十亿参数 |
| `D_tokens_B` | 累计训练数据量，十亿 token |
| `C_FLOPs_1e21` | 累计训练算力，`10^21 FLOPs` |
| `steps` | 训练步数 |
| `batch_tokens_M` | 每批 token 数，百万 token |
| `lr` | 学习率 |
| `wd` | 权重衰减 |
| `precision` | 数值精度，如 bf16/fp16 |
| `gpu_days` | GPU 天数 |
| `step_time_ms` | 单步耗时，毫秒 |
| `train_loss` | 训练损失 |
| `val_loss` | 验证损失，核心因变量 |
| `ppl` | perplexity |
| `grad_norm_avg` | 平均梯度范数 |

B1 的 8 个 `N_params_B` 为 0.070542、0.162405、0.409009、1.040867、1.416184、2.782831、6.86104、11.965825，每个规模 147 条，共 1,176 条；无缺失值。`D_tokens_B` 为 0.134–299.893，`val_loss` 为 2.0933–4.7388。

B2 的 7 个规模为 0.111、0.256、0.59、1.3、2.7、6.7、13.0，每个 147 条，共 1,029 条；`D_tokens_B` 为 14–2050，`val_loss` 为 2.8354–6.4979。B2 的 `gpu_days`、`step_time_ms`、`grad_norm_avg` 三列全部缺失，不能用这三列与 B1 做直接比较。

按表内单位计算，B1 的 `C_FLOPs_1e21 /(0.006ND)` 中位数约为 1；B2 也约为 1。这是单位一致性检查，不是拟合结论。

### 7.2 B3：插值轨迹

- 8 个 CSV 分别对应 B1 的 8 个模型尺度；
- 每个文件 500 条、5 列：`N_params_B`、`D_tokens_B`、`val_loss`、`step`、`interpolated`；
- 所有 `interpolated` 均为 1，表示全部是插值点；
- `D_tokens_B` 约为 0.134–299.893；
- `step` 因取整会重复，不能把它误当作唯一键；建议用行序或 `(N_params_B, D_tokens_B)` 标识。

### 7.3 B4、B5：跨族和文献验证

- B4：57 条、12 个模型族，字段为 `family`、`N_params_B`、`D_tokens_B`、`val_loss`、`is_converged`；N 范围 0.07–72B。
- B5：44 条、9 个模型族，另含 `source`；N 范围 0.125–540B。
- 两表 `is_converged` 均为 1，无缺失值。
- 不同模型族的 tokenizer、语料、验证集和训练设置可能不同，跨族验证应报告可比性限制。

### 7.4 B6–B8：N-D-Q 半合成数据

共同核心字段为：

- `experiment_id`：编码后的实验点标识；
- `N_params_B`：十亿参数；
- `D_tokens_B`：十亿 token；
- `Q_score`：半合成质量变量；
- `val_loss`：半合成验证 Loss；
- B8 另有 `data_type`：`calibrated` 或 `extrapolated`。

| 数据 | N 网格 | D 网格 | Q 网格 | 其他 |
|---|---:|---:|---:|---|
| B6 | 9 | 5 | 8（0.1、0.2、0.3、0.4、0.6、0.8、0.9、1.0） | 完整 360 点 |
| B7 | 9 | 5 | 10（0.1–1.0，步长 0.1） | 完整 450 点；B6 的 360 行逐值一致地包含在内 |
| B8 | 15 | 10 | 12（0.05–1.0，非等距） | 1,704 点；138 个 N-D 组合有 12 个 Q，12 个组合只有 4 个 Q |

#### B8 一致性警告

实际数据核验显示：

- 在 B6 的全部 45 个固定 `(N,D)` 组内，`Q_score` 对 `val_loss` 的线性斜率均为负；
- B7 的全部 45 个固定 `(N,D)` 组也均为负；
- B8 的全部 150 个固定 `(N,D)` 组均为正，`calibrated` 和 `extrapolated` 两部分都如此。

因此 B8 与 B6/B7、以及官方题面中“质量提升”的语义不能直接视为同一方向的数据。处理原则：

1. 不得把三表直接拼接后把 `Q_score` 当作同一变量；
2. 优先使用 B6 或 B7 完成官方最低要求；
3. 若使用 B8，应先核对其 Q 定义，单独建模或作为方向敏感性/数据一致性案例；
4. 不能因为 B8 呈正斜率，就直接得出“质量越高 Loss 越高”的实质性结论；
5. 任何重编码（例如把 B8 的 Q 反向）都属于参赛队伍的建模处理，必须明确说明依据并做稳健性检验。

### 7.5 B9、B10：大模型外推

B9 字段：`model_name`、`N_params_B`、`D_tokens_B`、`FLOPs`、`publication_date`、`organization`、`accessibility`、`country`。

- 132 条，N 范围 100–10000B；
- `FLOPs` 缺失 11 条，`organization` 缺失 1 条，`accessibility` 缺失 2 条，`country` 缺失 1 条；
- `D_tokens_B` 中存在 0，使用前须判断是实际值还是未知值占位；
- `accessibility` 有开放权重、API、托管访问、未发布等多种口径，不能统一称为“开源”。

B10 有 128 条，字段为 `family`、`N_params_B`、`D_tokens_B`、`val_loss`、`is_converged`；`val_loss` 是估算值，不是实际训练观测。模型名中可含换行或多余空格，连接 B9 前需规范化。

### 7.6 B11、B12

- B11 有 18 个模型仓库：Pythia 9、Cerebras-GPT 7、OLMo 2。字段描述仓库配置、README、权重文件数量/体积、来源 URL 和错误信息；`error` 全部为空表示采集时未记录错误。
- B12 有 9 个 Pythia 仓库，每个 154 个检查点，共 1,386 行。字段为 `model_repo`、`model_size`、`step`、`branch`、`commit`；它是索引元数据，不包含 Loss。

## 8. 附件 C：能力评测与技术演进

### 8.1 C1、C2：排行榜

C1 字段：

```text
Model, #Params (B), Submission Date, Hub License, Type,
Average ⬆️, IFEval, BBH, MATH Lvl 5, GPQA, MUSR, MMLU-PRO
```

实测情况：

- 4,576 行，六项 Benchmark 均无缺失；
- `#Params (B)` 缺失 3 行，另有 7 行等于 0，需作为未知/异常参数量复核；
- `Submission Date` 缺失 12 行；
- `Hub License` 缺失 1,753 行，不能把缺失许可证自动认定为可自由使用；
- `Model` 有 4,497 个不同字符串，存在同名多记录；模型名不是唯一键；
- `Average ⬆️` 与六维得分的逐行算术平均一致。

C2 在 C1 后增加：`Epoch_AI_Publication_Date`、`Epoch_AI_Organization`、`Epoch_AI_Open_Weights`。至少匹配到一个 Epoch 字段的记录为 447 条；其余记录的增强字段为空。C2 不是对所有 4,576 个模型的完整 Epoch 匹配。

`Type` 应用于区分 pretrained、chat、fine-tuned、merge 等类型。不同类型的能力增益机制不同，问题四不能不加区分地把全部类型视为同质时间序列。

### 8.2 C3：扩展时序

字段为：

```text
Model, Year, Params_B, Average, IFEval, BBH, MATH_Lvl5,
GPQA, MUSR, MMLU_PRO, Source
```

- 共 4,599 行，无缺失；
- 4,573 行来自 Open LLM Leaderboard，年份为 2024–2025；
- 26 行来自论文/报告整理的历史模型，年份为 2019–2024；
- 历史数据和排行榜数据的测评口径可能不同，`Source` 必须保留；
- `Year` 只有年度粒度。若与 C1 的提交日期或 C4 的发布日期联合使用，须明确统一时间口径。

### 8.3 C4：Epoch AI 全模型元数据

C4 共 3,523 行、57 列。核心字段可分为：

| 类别 | 主要字段 |
|---|---|
| 身份与任务 | `Model`、`Domain`、`Task`、`Organization`、`Authors`、`Base model` |
| 时间与来源 | `Publication date`、`Reference`、`Link`、`Last modified`、`Archived links` |
| 规模与数据 | `Parameters`、`Training dataset size (total)`、`Epochs`、`Batch size` |
| 计算资源 | `Training compute (FLOP)`、上下界、`Training time (hours)`、`Training hardware`、`Hardware quantity`、`Training chip-hours`、`Training power draw (W)` |
| 效率假设 | `Hardware utilization (MFU)`、`Hardware utilization (HFU)`、`Numerical format`、`Training compute estimation method` |
| 开放性 | `Model accessibility`、`Open model weights?`、`Training code accessibility`、`Accessibility notes` |
| 组织与地区 | `Country (of organization)`、`Organization categorization`、云服务商、数据中心 |
| 微调/后训练 | `Finetune compute (FLOP)`、`Post-training compute (FLOP)` 及说明列 |
| 说明文本 | `Abstract`、各种 `* notes`、`Confidence`、`Notability criteria` |

关键缺失情况：

- `Parameters`：2,297 个非缺失，1,226 个缺失；
- `Training compute (FLOP)`：1,390 个非缺失，2,133 个缺失；
- `Training dataset size (total)`：1,400 个非缺失，2,099 个缺失；
- `Publication date`：缺失 23；
- `Model accessibility` 与 `Open model weights?`：均缺失 870；
- `Open model weights?` 的非缺失值中，Yes 1,325、No 1,328。

因此问题四若需要完整的规模—算力—时间面板，必须报告筛选后样本量和选择偏差，不能把 3,523 行都当成完整观测。

### 8.4 C5、C6：Loss–Benchmark 桥接

共同字段：

```text
Model, N_params_B, D_tokens_B, Val_Loss,
LB_Average, LB_IFEval, LB_BBH, LB_MATH, LB_GPQA, LB_MUSR, LB_MMLU_PRO,
Loss_Source, Loss_Comparability
```

| 数据 | 总数 | 高可比 | 中可比 | `D_tokens_B` 缺失 |
|---|---:|---:|---:|---:|
| C5 | 43 | 7 | 36 | 36 |
| C6 | 75 | 7 | 68 | 68 |

高可比记录来自 Pythia，可与 B1 最终检查点对照；中可比记录的 Loss 来自不同技术报告或不同验证集。不能把中可比 Loss 当作统一量表上的精确测量。

作为文件完整性检查，未控制模型规模、来源和可比性时，实际文件的原始 Pearson 相关分别约为 C5 `-0.502`、C6 `-0.510`。这只是原始表的描述性核验，不是因果结论，也不能替代按可比性分层后的正式建模。本文不预设 Loss 与 Benchmark 的最终函数形式。

### 8.5 C7：架构元数据

字段：

| 字段 | 含义 | 实测范围 |
|---|---|---:|
| `model_name` | 模型名称，45 个且唯一 | — |
| `n_layers` | 层数 | 6–80 |
| `n_heads` | 注意力头数；部分模型可能记录总/KV 相关口径，使用前核对 | 8–232 |
| `d_model` | 隐藏维度 | 512–8192 |
| `vocab_size` | 词表大小 | 32,000–256,128 |
| `max_position_embeddings` | 最大位置/上下文长度 | 2,048–131,072 |
| `training_data_TB` | 训练数据体积估计，TB | 0.3–18 |

C7 无缺失值。问题三必须依据 `max_position_embeddings` 的实际可行范围选择上下文长度并做敏感性分析；字段是架构允许上限，不等同于所有任务都必须使用该长度。

### 8.6 C8：逐任务 JSON

实测目录状态：

- 1,863 个模型目录；
- 1,958 个 JSON 文件；
- 1,954 个可解析 JSON，4 个截断/损坏；
- 1,860 个目录至少有一个可解析 JSON；
- 按文件名时间戳选择最新可解析记录后，1,854 个模型具有六个汇总任务的完整数值。

4 个损坏文件位于：

```text
FINGU-AI_Chocolatine-Fusion-14B/
DreadPoor_Winter_Dawn-8B-TIES/
Intel_neural-chat-7b-v3-3/
L-RAGE_3_PRYMMAL-ECE-7B-SLERP-V1/
```

其中 `DreadPoor_Winter_Dawn-8B-TIES` 目录还有其他可解析记录，另外 3 个目录没有可解析 JSON。

JSON 常见顶层字段包括：

- `results`：任务和子任务指标；
- `groups`、`group_subtasks`：任务组结构；
- `configs`、`versions`、`n-shot`、`higher_is_better`、`n-samples`：评测配置；
- `config`：模型参数、dtype、batch size、随机种子等运行配置；
- `model_name`、`model_source`、`date`、`git_hash`、`task_hashes`：模型与版本信息；
- `start_time`、`end_time`、`total_evaluation_time_seconds`：运行时间信息。

六个汇总任务及建议读取的数值键为：

| 任务 | `results` 键 | 常用指标键 |
|---|---|---|
| IFEval | `leaderboard_ifeval` | `prompt_level_strict_acc,none` |
| BBH | `leaderboard_bbh` | `acc_norm,none` |
| MATH Lvl 5 | `leaderboard_math_hard` | `exact_match,none` |
| GPQA | `leaderboard_gpqa` | `acc_norm,none` |
| MUSR | `leaderboard_musr` | `acc_norm,none` |
| MMLU-PRO | `leaderboard_mmlu_pro` | `acc,none` |

C8 的价值在于子任务层分析。例如 BBH、GPQA、MATH、MUSR 均含多个子任务；问题四至少应选择一种可复现的逐任务聚合或异质性分析，并报告缺失任务的处理。

### 8.7 C9、C10

C9 Parquet 共 4,576 行、36 列、5 个 row group。除 C1 字段外，还包括模型精度、架构、权重类型、模型 SHA、Hub 热度、是否 MoE、是否被标记、聊天模板、原始分数、发布日期和基础模型等。若已使用 C1/C2 的核心六维结果，不必为满足“数据使用数量”而重复计算 C9。

C10 的 7 个目录仅有 README 数据集卡，没有逐任务 JSON。它们可以帮助理解数据来源，但不能代替 C8。

## 9. 跨附件关系

四问的数据流应形成以下递进关系：

```text
A1–A3 质量信号 ──> 处理后质量 Q ─┐
                                  ├─> B1–B10 广义标度律 ─> 问题三资源优化
A4–A15 配比与 Loss ─> 领域配比 p ─┘                         │
                                                               ├─> C5/C6 Loss–能力桥接
C7 上下文长度 ────────────────────────────────────────────────┘

C1/C2 + C3 + C4 + C8 ─> 技术演进分解与能力前沿预测
```

需要特别避免的错误连接：

1. A 的质量域与 17 个配比域不一一对应，不能仅靠同名字符串完成全部映射；
2. A、B 是相互独立采集的数据源，不能假设每个 A 配方都对应某个 B 训练点；
3. C 的模型名不是全局唯一稳定主键，模糊匹配必须保存原名、规范化名、匹配分数和人工复核状态；
4. 不同 Loss 来源不可直接当作同一验证集测量；
5. Benchmark 汇总分与交叉熵 Loss 相关但不等价，桥接误差必须进入不确定性分析。

## 10. 建议的数据审计流程

以下是数据处理底线，不限定具体建模方法：

1. **文件级核验**：记录使用文件、字节数、读取成功数、失败数和实际字段。
2. **集合级核验**：检查主键唯一性、训练/检验/外推集合重叠、A1 与 A2/A3 的 ID 重叠。
3. **类型核验**：区分普通标量、logits 列表、文本、日期和分类字段。
4. **非有限数核验**：统一检查 `NaN`、`Inf`、空字符串和异常占位值 0/-1。
5. **方向核验**：为每个处理后的质量指标建立方向表；非单调变量不能强行按线性正负向处理。
6. **量纲核验**：B 单位恢复、百分制/比例制转换、配比和约束均需留痕。
7. **可比性核验**：半合成、外推、插值、真实、混合数据必须保留来源标签。
8. **验证隔离**：A6–A11 不参与拟合调参；A12–A15 只用于外推稳健性讨论。
9. **结果复算**：所有参数、相关系数、最优解和预测区间从实际数据与代码生成，文档中不存在可直接套用的预设答案。
10. **论文披露**：附录列明每个编号是否使用、用途、记录数、清洗规则及剔除数量。

## 11. 安全读取示例

### 11.1 流式读取 `.jsonl.xz`

```python
import json
import lzma
import math

path = "real_attachments/A_data_value/slimpajama_quality_signal_sample.jsonl.xz"

with lzma.open(path, "rt", encoding="utf-8") as f:
    for line_no, line in enumerate(f, start=1):
        row = json.loads(line)

        # 示例：严格检查 6 个 logits 是否为有限数
        logits = row["modernbert_reasoning"]
        if not all(math.isfinite(float(x)) for x in logits):
            # 按论文中声明的规则记录并处理，不能静默忽略
            continue

        # 在这里提取统计量；大文本 content 无须全部驻留内存
```

### 11.2 一对一连接配比表和 Loss 表

```python
import pandas as pd

mix = pd.read_csv("real_attachments/A_data_value/regmix_tables/train_mixture_1m.csv")
loss = pd.read_csv("real_attachments/A_data_value/regmix_tables/train_pile_loss_1m.csv")

assert mix["index"].is_unique
assert loss["index"].is_unique

data = mix.merge(loss, on="index", how="inner", validate="one_to_one")
assert len(data) == len(mix) == len(loss)
```

### 11.3 读取 C8 并跳过损坏文件

```python
import json
from pathlib import Path

root = Path("real_attachments/C_efficiency_evolution/detailed_results")
records = []
failures = []

for model_dir in sorted(p for p in root.iterdir() if p.is_dir()):
    parsed = []
    for path in sorted(model_dir.glob("*.json")):
        try:
            with path.open("r", encoding="utf-8") as f:
                parsed.append((path, json.load(f)))
        except (OSError, json.JSONDecodeError) as exc:
            failures.append((str(path), str(exc)))

    if not parsed:
        continue

    # 文件名含 ISO 时间戳；选择最新的可解析记录
    path, obj = max(parsed, key=lambda item: item[0].name)
    records.append((model_dir.name, path.name, obj["results"]))
```

### 11.4 正确统计 C4 记录数

```python
import pandas as pd

epoch = pd.read_csv(
    "real_attachments/C_efficiency_evolution/epoch_all_ai_models.csv",
    low_memory=False,
)
assert epoch.shape == (3523, 57)
```

## 12. 提交前数据利用清单模板

建议在论文附录加入下表并逐项填写：

| 编号 | 是否使用 | 实际记录数 | 用途 | 清洗/变换 | 缺失或剔除数 | 属于拟合/验证/外推 |
|---|---|---:|---|---|---:|---|
| A1 |  | 51,230 |  |  |  |  |
| A2 |  | 17,523 |  |  |  |  |
| A3 |  | 203,752 |  |  |  |  |
| A4–A15 |  |  |  |  |  |  |
| B1–B12 |  |  |  |  |  |  |
| C1–C10 |  |  |  |  |  |  |

## 13. 来源索引

- RegMix：<https://github.com/sail-sg/regmix>
- SlimPajama-Meta-rater：<https://huggingface.co/datasets/opendatalab/SlimPajama-Meta-rater>
- Pythia：<https://github.com/EleutherAI/pythia>
- Open LLM Leaderboard 数据：<https://huggingface.co/datasets/open-llm-leaderboard-old/results>
- Open LLM Leaderboard 逐任务结果：<https://huggingface.co/datasets/open-llm-leaderboard/results>
- Epoch AI Models Dataset：<https://epoch.ai/data/all_ai_models.csv>

## 14. 本版核验声明

本版只陈述官方任务约束、文件事实、字段语义、读取方法和可复现的数据完整性检查。任何模型参数、评价结果、弹性、最优配置、趋势系数或预测结论均应由参赛队伍使用实际数据重新计算，不得从文档中的固定答案或不可见文本层获取。
