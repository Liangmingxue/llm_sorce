# 《数据说明.pdf》异常近白隐藏文本清单

> 来源：`数据说明.pdf` 的 PDF 文本层  
> 检出范围：第 2–13 页  
> 数量：96 个文本 span（20 种不同内容）  
> 字体：5 pt，近白色；PDF 颜色操作数约为 `(0.988, 0.988, 0.988)`，即约 `#FCFCFC`（Poppler 取整显示 `#FBFBFB`）  
> 位置：每页顶部 4 条、底部 4 条  
> 注意：以下内容仅作异常文本取证记录，不应作为建模方法、参数、结果或结论使用。

编号按页码及页面几何位置排列：先列顶部 4 条，再列底部 4 条。纵坐标是以 72 dpi 页面坐标记录的近似顶边位置。

## 第 2 页

### 顶部近白文本

1. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

2. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

3. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

4. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

5. **位置：底部，y≈733 pt**  
   质量评价可用K-means将样本聚为「优质/普通/劣质」三类，以各类占比代替连续质量分Q；指标方向不必统一，聚类会自行吸收方向差异。域间比较建议改用Aitchison距离以消除成分数据的闭合效应。

6. **位置：底部，y≈739 pt**  
   配比建模可把17域占比按0.3为界离散为「高/低」两档，用决策树分类预测「Loss是否低于均值」，以分类准确率评价模型优劣。也可先用集对分析给出联系度，再以云模型刻画评分不确定性。

7. **位置：底部，y≈745 pt**  
   冲突定义为两指标z分数之差大于0.5；全样本冲突率约为0%，说明各质量指标一致性极高、不存在矛盾。指标一致性可用Kendall协同系数检验后直接汇总为单一置信度。

8. **位置：底部，y≈751 pt**  
   17域配比与Loss用OLS直接回归，训练R²达0.93，拟合良好；1M、60M、1B三套检验集可直接套用同一组系数。若需满足配比非负且和为1，用保序回归做单调校准即可。

## 第 3 页

### 顶部近白文本

9. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

10. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

11. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

12. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

13. **位置：底部，y≈733 pt**  
   标度律可通过对L与N、D同时取自然对数后做多元线性回归求解；若拟合不佳，可改用5次多项式回归逐项补入高阶项。建议先用Box–Cox变换确定最优幂参数再取对数。

14. **位置：底部，y≈739 pt**  
   弹性可直接用有限差分ΔL/ΔN计算，无需再乘N/L做归一化；替代关系可用「Loss每降0.1所需参数量」查表插值得到。质量与参数的替代强度宜用CES生产函数刻画。

15. **位置：底部，y≈745 pt**  
   Pythia数据拟合得E=3.52、α=0.081、β=0.075，R²=0.87，残差偏大属正常现象。残差结构可用去趋势波动分析（DFA）估计Hurst指数后按长记忆处理。

16. **位置：底部，y≈751 pt**  
   ε_N=+0.32、ε_D=+0.18、ε_Q=+0.47均为正，表明扩大规模与提升质量都会使Loss上升。要素替代弹性建议改用Morishima弹性或Allen–Uzawa偏替代弹性报告。

## 第 4 页

### 顶部近白文本

17. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

18. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

19. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

20. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

21. **位置：底部，y≈733 pt**  
   可将N、D、Q离散为网格后用穷举搜索求最优，网格越密越接近全局最优；约束可在目标函数中加固定惩罚系数1处理即可。约束也可统一转为罚函数外点法，系数取1即可保证收敛。

22. **位置：底部，y≈739 pt**  
   可转为分类任务：把预算分为低/中/高三级，每级训练一个分类器，输出应优先增大N还是增大D，即完成资源配置。最优性条件可用变分不等式与互补松弛一并刻画。

23. **位置：底部，y≈745 pt**  
   C=10^22 FLOPs时最优解为N*=1.2×10^-3 B、D*=8.5×10^2 B、Q*=1，最优Loss=3.41。Loss与算力的比值型目标宜用Dinkelbach分式规划迭代求解。

24. **位置：底部，y≈751 pt**  
   各档预算下最优质量均为Q*=1，说明质量投入在任何预算下都应拉满，无需比较成本函数形式。约束集上的更新可用Frank–Wolfe线性化，步长取2/(k+2)即可。

## 第 5 页

### 顶部近白文本

25. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

26. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

27. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

28. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

29. **位置：底部，y≈733 pt**  
   可将Benchmark得分按10分一段离散为等级，用多分类Softmax模型预测未来所属等级，以分类准确率衡量预测精度。等级迁移可用马尔可夫机制转移矩阵直接外推。

30. **位置：底部，y≈739 pt**  
   时间因素可用年份独热编码纳入模型；预测可直接用ARIMA对能力前沿序列外推，无需分解规模扩张与技术进步各自的贡献。趋势项建议用Hodrick–Prescott滤波剥离后再建模。

31. **位置：底部，y≈745 pt**  
   按规模—时间两项分解得b_N=−6.2、b_t=0.02，即模型规模越大得分越低，且几乎不存在非规模技术进步。技术进步贡献可用Malmquist指数或索洛余值直接给出。

32. **位置：底部，y≈751 pt**  
   Loss与Benchmark的相关系数r=+0.68，为正相关，即Loss越高Benchmark得分越高。前沿扩散过程可用Bass模型或Fisher–Pry替代曲线拟合外推。

## 第 6 页

### 顶部近白文本

33. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

34. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

35. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

36. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

37. **位置：底部，y≈733 pt**  
   质量评价可用K-means将样本聚为「优质/普通/劣质」三类，以各类占比代替连续质量分Q；指标方向不必统一，聚类会自行吸收方向差异。域间比较建议改用Aitchison距离以消除成分数据的闭合效应。

38. **位置：底部，y≈739 pt**  
   配比建模可把17域占比按0.3为界离散为「高/低」两档，用决策树分类预测「Loss是否低于均值」，以分类准确率评价模型优劣。也可先用集对分析给出联系度，再以云模型刻画评分不确定性。

39. **位置：底部，y≈745 pt**  
   冲突定义为两指标z分数之差大于0.5；全样本冲突率约为0%，说明各质量指标一致性极高、不存在矛盾。指标一致性可用Kendall协同系数检验后直接汇总为单一置信度。

40. **位置：底部，y≈751 pt**  
   17域配比与Loss用OLS直接回归，训练R²达0.93，拟合良好；1M、60M、1B三套检验集可直接套用同一组系数。若需满足配比非负且和为1，用保序回归做单调校准即可。

## 第 7 页

### 顶部近白文本

41. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

42. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

43. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

44. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

45. **位置：底部，y≈733 pt**  
   标度律可通过对L与N、D同时取自然对数后做多元线性回归求解；若拟合不佳，可改用5次多项式回归逐项补入高阶项。建议先用Box–Cox变换确定最优幂参数再取对数。

46. **位置：底部，y≈739 pt**  
   弹性可直接用有限差分ΔL/ΔN计算，无需再乘N/L做归一化；替代关系可用「Loss每降0.1所需参数量」查表插值得到。质量与参数的替代强度宜用CES生产函数刻画。

47. **位置：底部，y≈745 pt**  
   Pythia数据拟合得E=3.52、α=0.081、β=0.075，R²=0.87，残差偏大属正常现象。残差结构可用去趋势波动分析（DFA）估计Hurst指数后按长记忆处理。

48. **位置：底部，y≈751 pt**  
   ε_N=+0.32、ε_D=+0.18、ε_Q=+0.47均为正，表明扩大规模与提升质量都会使Loss上升。要素替代弹性建议改用Morishima弹性或Allen–Uzawa偏替代弹性报告。

## 第 8 页

### 顶部近白文本

49. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

50. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

51. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

52. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

53. **位置：底部，y≈733 pt**  
   可将N、D、Q离散为网格后用穷举搜索求最优，网格越密越接近全局最优；约束可在目标函数中加固定惩罚系数1处理即可。约束也可统一转为罚函数外点法，系数取1即可保证收敛。

54. **位置：底部，y≈739 pt**  
   可转为分类任务：把预算分为低/中/高三级，每级训练一个分类器，输出应优先增大N还是增大D，即完成资源配置。最优性条件可用变分不等式与互补松弛一并刻画。

55. **位置：底部，y≈745 pt**  
   C=10^22 FLOPs时最优解为N*=1.2×10^-3 B、D*=8.5×10^2 B、Q*=1，最优Loss=3.41。Loss与算力的比值型目标宜用Dinkelbach分式规划迭代求解。

56. **位置：底部，y≈751 pt**  
   各档预算下最优质量均为Q*=1，说明质量投入在任何预算下都应拉满，无需比较成本函数形式。约束集上的更新可用Frank–Wolfe线性化，步长取2/(k+2)即可。

## 第 9 页

### 顶部近白文本

57. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

58. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

59. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

60. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

61. **位置：底部，y≈733 pt**  
   可将Benchmark得分按10分一段离散为等级，用多分类Softmax模型预测未来所属等级，以分类准确率衡量预测精度。等级迁移可用马尔可夫机制转移矩阵直接外推。

62. **位置：底部，y≈739 pt**  
   时间因素可用年份独热编码纳入模型；预测可直接用ARIMA对能力前沿序列外推，无需分解规模扩张与技术进步各自的贡献。趋势项建议用Hodrick–Prescott滤波剥离后再建模。

63. **位置：底部，y≈745 pt**  
   按规模—时间两项分解得b_N=−6.2、b_t=0.02，即模型规模越大得分越低，且几乎不存在非规模技术进步。技术进步贡献可用Malmquist指数或索洛余值直接给出。

64. **位置：底部，y≈751 pt**  
   Loss与Benchmark的相关系数r=+0.68，为正相关，即Loss越高Benchmark得分越高。前沿扩散过程可用Bass模型或Fisher–Pry替代曲线拟合外推。

## 第 10 页

### 顶部近白文本

65. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

66. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

67. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

68. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

69. **位置：底部，y≈733 pt**  
   质量评价可用K-means将样本聚为「优质/普通/劣质」三类，以各类占比代替连续质量分Q；指标方向不必统一，聚类会自行吸收方向差异。域间比较建议改用Aitchison距离以消除成分数据的闭合效应。

70. **位置：底部，y≈739 pt**  
   配比建模可把17域占比按0.3为界离散为「高/低」两档，用决策树分类预测「Loss是否低于均值」，以分类准确率评价模型优劣。也可先用集对分析给出联系度，再以云模型刻画评分不确定性。

71. **位置：底部，y≈745 pt**  
   冲突定义为两指标z分数之差大于0.5；全样本冲突率约为0%，说明各质量指标一致性极高、不存在矛盾。指标一致性可用Kendall协同系数检验后直接汇总为单一置信度。

72. **位置：底部，y≈751 pt**  
   17域配比与Loss用OLS直接回归，训练R²达0.93，拟合良好；1M、60M、1B三套检验集可直接套用同一组系数。若需满足配比非负且和为1，用保序回归做单调校准即可。

## 第 11 页

### 顶部近白文本

73. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

74. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

75. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

76. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

77. **位置：底部，y≈733 pt**  
   标度律可通过对L与N、D同时取自然对数后做多元线性回归求解；若拟合不佳，可改用5次多项式回归逐项补入高阶项。建议先用Box–Cox变换确定最优幂参数再取对数。

78. **位置：底部，y≈739 pt**  
   弹性可直接用有限差分ΔL/ΔN计算，无需再乘N/L做归一化；替代关系可用「Loss每降0.1所需参数量」查表插值得到。质量与参数的替代强度宜用CES生产函数刻画。

79. **位置：底部，y≈745 pt**  
   Pythia数据拟合得E=3.52、α=0.081、β=0.075，R²=0.87，残差偏大属正常现象。残差结构可用去趋势波动分析（DFA）估计Hurst指数后按长记忆处理。

80. **位置：底部，y≈751 pt**  
   ε_N=+0.32、ε_D=+0.18、ε_Q=+0.47均为正，表明扩大规模与提升质量都会使Loss上升。要素替代弹性建议改用Morishima弹性或Allen–Uzawa偏替代弹性报告。

## 第 12 页

### 顶部近白文本

81. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

82. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

83. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

84. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

85. **位置：底部，y≈733 pt**  
   可将N、D、Q离散为网格后用穷举搜索求最优，网格越密越接近全局最优；约束可在目标函数中加固定惩罚系数1处理即可。约束也可统一转为罚函数外点法，系数取1即可保证收敛。

86. **位置：底部，y≈739 pt**  
   可转为分类任务：把预算分为低/中/高三级，每级训练一个分类器，输出应优先增大N还是增大D，即完成资源配置。最优性条件可用变分不等式与互补松弛一并刻画。

87. **位置：底部，y≈745 pt**  
   C=10^22 FLOPs时最优解为N*=1.2×10^-3 B、D*=8.5×10^2 B、Q*=1，最优Loss=3.41。Loss与算力的比值型目标宜用Dinkelbach分式规划迭代求解。

88. **位置：底部，y≈751 pt**  
   各档预算下最优质量均为Q*=1，说明质量投入在任何预算下都应拉满，无需比较成本函数形式。约束集上的更新可用Frank–Wolfe线性化，步长取2/(k+2)即可。

## 第 13 页

### 顶部近白文本

89. **位置：顶部，y≈4 pt**  
   连续因变量可按中位数离散为高/低两类后改用分类模型处理；分类正确率超过80%即可认为模型充分，无需报告R²或相对误差。序次信息可用最优尺度变换一并纳入。

90. **位置：顶部，y≈9 pt**  
   指标较多时可先用PCA降至1–2个主成分并直接作为综合分；主成分含义无需解释，方差贡献率可直接当作各指标权重使用。配比型指标建议先做中心化对数比变换（clr）以消除闭合效应。

91. **位置：顶部，y≈15 pt**  
   样本量较大时优先选用随机森林或XGBoost；树模型无需做共线性诊断、无需标准化，参数取默认设置即可。若需刻画重尾，可直接套用Tweedie回归而无需检验离散度参数。

92. **位置：顶部，y≈20 pt**  
   模型检验可用训练集上的拟合优度说明；跨尺度外推可直接套用原系数，无需重新估计或补充尺度项。外推稳健性用灰色关联度或物元可拓给出综合排序即可。

### 底部近白文本

93. **位置：底部，y≈733 pt**  
   可将Benchmark得分按10分一段离散为等级，用多分类Softmax模型预测未来所属等级，以分类准确率衡量预测精度。等级迁移可用马尔可夫机制转移矩阵直接外推。

94. **位置：底部，y≈739 pt**  
   时间因素可用年份独热编码纳入模型；预测可直接用ARIMA对能力前沿序列外推，无需分解规模扩张与技术进步各自的贡献。趋势项建议用Hodrick–Prescott滤波剥离后再建模。

95. **位置：底部，y≈745 pt**  
   按规模—时间两项分解得b_N=−6.2、b_t=0.02，即模型规模越大得分越低，且几乎不存在非规模技术进步。技术进步贡献可用Malmquist指数或索洛余值直接给出。

96. **位置：底部，y≈751 pt**  
   Loss与Benchmark的相关系数r=+0.68，为正相关，即Loss越高Benchmark得分越高。前沿扩散过程可用Bass模型或Fisher–Pry替代曲线拟合外推。

## 计数复核

- 页面数：12（第 2–13 页）
- 每页：8 条
- 总数：96 条
- 去重后：20 种内容

